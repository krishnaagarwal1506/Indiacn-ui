---
title: Radar Chart
description: "One subject scored across several dimensions."
---

## Introduction

A radar chart shows a profile — the shape matters more than any single axis.

All chart types share one component. Installation, the palette, and the reason every chart ships with a screen-reader data table are on the [Charts](/docs/chart) page.

## Example

<ComponentPreview name='ChartRadar' />

## When to use it

Use it to compare two or three profiles across the same dimensions, such as this quarter against last. Keep the axes to a handful and the scales identical, or the shape misleads.

## Usage

<ComponentSource collapsible={false} code={`import { Chart } from '@/components/ui/chart';`} />

See the [Charts](/docs/chart) page for the full API.
